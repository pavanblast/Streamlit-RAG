# Core LLM and RAG stack
langchain>=0.1.14
langchain-huggingface>=0.0.1
sentence-transformers>=2.2.2
faiss-cpu>=1.7.4

# LLM APIs
openai>=1.23.2  # Required for ChatOpenAI, even for DeepSeek compatible clients

# Web interface
streamlit>=1.33.0

# Pytest for test execution
pytest>=7.4.0

# Utility
tqdm
numpy>=1.24.0

# Optional (if using huggingface models or downloading from HF)
transformers>=4.40.0
torch>=2.0.0

# Use uvicorn only if running as FastAPI app later
# uvicorn[standard]  # Optional
